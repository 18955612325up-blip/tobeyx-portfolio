# 网站检查与空间漫游加载修复

日期：2026-09-05。范围：本地源码与正式构建预览；未检查线上主机/CDN 配置。

## 已确认并修复

| 问题 | 证据与处理 |
| --- | --- |
| 解码依赖外部服务 | 阻断 gstatic.com 后，原实现停在 83%，并不断产生 Failed to fetch。改为同站点解码文件，网络失败统一进入可重试的错误状态。 |
| 等待反馈依赖 WebGL | 原提示位于 3D 画布内部。新增独立网页预览，覆盖下载、解码和首帧准备；完成后淡出。 |
| 进度不代表实际下载量 | 原进度按资源个数计算。现在显示模型已接收字节数；只有文件总长度可靠时才显示百分比。 |
| 首页加载过重 | 首页原始程序包约 1,501 KB，拆分后约 230 KB，减少约 85%。下方图片按需加载，首页不下载模型。 |
| 再次进入重复处理模型 | 复用同一页面会话中已解码的模型和整理后的几何数据；关闭时销毁画布，避免继续占用绘制循环。 |
| 弹窗背景仍可滚动、键盘可离开 | 增加背景滚动锁定、背景 inert、弹窗焦点循环、关闭后恢复入口焦点。 |
| 效果图点击取消自身选择 | 原先 focus 与 click 两次改变同一状态。改为点击明确选中，手机点按后说明保留。 |
| 编辑工具暴露给访客 | 按项目既有规范，校准入口仅在开发预览的 `?calibrate` 中显示；保存的视角、名称和位置继续读取。 |

## 验证

- 生产构建通过；正常浏览与漫游回归未出现未捕获脚本异常。
- 本机一轮生产预览：约 77 ms 出现等待预览，约 3.39 s 进入模型，重复进入约 0.86 s；同一会话只有一次模型请求。这些不是公网速度承诺。
- 阻断解码文件后出现错误与重试入口；恢复网络后重试成功。
- 模拟 WebGL 上下文丢失后可重试；不支持 WebGL 时可返回图文。
- 约 4 Mbps 下载限速下显示真实进度；15 秒出现较慢提示；加载中关闭、再次进入成功。
- 320、390、768、1280 px 视口无页面横向溢出；手机效果图选中与全部可见图片解码通过。
- 加载期间与完成后的键盘焦点、Escape 退出、背景滚动恢复通过。

回归脚本：`scripts/verify-site.cjs`、`scripts/verify-slow-mobile.cjs`。
截图：`audit-captures/slow-mobile-after.png`、`audit-captures/scene-after.png`、`audit-captures/scene-mobile-after.png`、`audit-captures/error-after.png`。

## 仍有的性能边界

原模型保留，大小为 25,311,956 字节，约 25.3 MB（24.1 MiB），已使用 Draco 几何压缩。弱网下载和较弱手机的解码、绘制仍有成本。3D 分包约 1.28 MB，构建仍会提示该按需分包较大。

如需继续降低首次等待，下一步应制作经过画面对比的轻量模型：先处理高面数植被、重复网格与远景建筑，再引入近远不同精度。线上缓存策略和真实低端手机帧率需要在部署环境另行验证。
